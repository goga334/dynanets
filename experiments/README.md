# Experiments

Place runnable experiment definitions here.

Suggested convention:

- `examples/` for reference configs
- `baselines/` for fixed-topology experiments
- `dynamic/` for structure-adaptive experiments
- `nas/` for search-driven experiments
